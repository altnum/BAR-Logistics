export default class EditUser {
  constructor (id, username, email, address, firstname, lastname) {
    this.id = id
    this.username = username
    this.email = email
    this.firstname = firstname
    this.lastname = lastname
    this.address = address
  }
}
